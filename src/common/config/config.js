const config = {
  SUCCESS_FLAG: 1,
  FAIL_FLAG: -1,
  LOGIN_TOKEN: false,
  TOKEN_NAME: 'token',
  DEFAULT_PAGE_NUM: 10, // 默认列表每页显示条数
  DEFAULT_PAGES_ARRAY: [10, 20, 30, 50], // 列表选择每页条数配置
};

export default config;
